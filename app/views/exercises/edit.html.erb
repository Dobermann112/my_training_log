<h2 class="mb-4">「<%= @exercise.name %>」を編集</h2>

<div class="exercise-title-card p-4 mb-4">
  <%= render "form", exercise: @exercise %>
</div>

<div class="d-flex gap-2">
  <%= link_to "一覧に戻る",
              body_part_exercises_path(@body_part, date: @date),
              class: "btn btn-outline-light" %>
</div>
