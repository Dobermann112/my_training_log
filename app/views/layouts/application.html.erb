<!DOCTYPE html>
<html>
  <head>
    <title>トレログ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="manifest" href="/manifest.json">
    
    <% unless user_signed_in? %>
      <meta property="og:title" content="トレログ | トレーニングを「続けられる」記録アプリ" />
      <meta property="og:description" content="筋トレ・ワークアウトをカレンダー形式で記録。部位別・種目別に振り返りができるトレーニングログアプリ。" />
      <meta property="og:type" content="website" />
      <meta property="og:url" content="<%= unauthenticated_root_url %>" />
      <meta property="og:image" content="<%= asset_url('ogp.png') %>" />

      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content="トレログ | トレーニングを「続けられる」記録アプリ" />
      <meta name="twitter:description" content="筋トレ・ワークアウトをカレンダー形式で記録。部位別・種目別に振り返りができるトレーニングログアプリ。" />
      <meta name="twitter:image" content="<%= image_url('ogp.png') %>" />    
    <% end %>
  </head>

  <body class="app-body <%= yield :body_class %>">
    <header>
      <%= render "shared/navbar_mobile" %>
      <%= render "shared/navbar" %>
    </header>

    <main class="container py-4">
      <div id="flash-area" aria-live="polite" aria-atomic="true">
        <%= render "shared/flash_messages" %>
      </div>

      <%= yield %>
    </main>

    <%= render "shared/footer_nav" %>
    <%= render "shared/footer" if lookup_context.exists?("shared/_footer") %>
  </body>
</html>
