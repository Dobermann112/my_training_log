<nav class="footer-nav d-md-none fixed-bottom bg-dark text-light border-top border-warning">
  <div class="footer-nav__inner">
    <%= link_to calendars_path, class: "text-light #{'active' if current_page?(calendars_path)}" do %>
      <i data-lucide="calendar"></i>
      <span>カレンダー</span>
    <% end %>
    <%= link_to dashboard_report_path, class: "text-light #{'active' if current_page?(dashboard_report_path)}" do %>
      <i data-lucide="bar-chart"></i>
      <span>レポート</span>
    <% end %>
    <%= link_to user_path, class: "text-light #{'active' if current_page?(user_path)}" do %>
      <i data-lucide="user"></i>
      <span>プロフィール</span>
    <% end %>
    <%= link_to "#", class: "text-light" do %>
      <i data-lucide="settings"></i>
      <span>設定</span>
    <% end %>
  </div>
</nav>
